name: Build, Push and Deploy Node.js app

# アクションを実行するタイミングを制御します。
on:
  # プッシュイベントが発生したときにワークフローを起動するが、その対象は mainブランチのみとする
  push:
    branches: [main]

  # [Actions] タブから手動でワークフローを実行することができます。
  workflow_dispatch: